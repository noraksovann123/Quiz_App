<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#FFFFFF">

    <!-- Top Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="10/10"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginEnd="8dp" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Drop Pin"
            android:fontFamily="@font/poppinssemibold"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center" />

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/menu"
            android:contentDescription="Menu Icon" />
    </LinearLayout>

    <!-- Progress Bar -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_marginBottom="16dp">

        <ProgressBar
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:progress="20"
            android:max="100"
            android:progressTint="#4CAF50"
            android:backgroundTint="#E0E0E0" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="20"
            android:textSize="10sp"
            android:textColor="#000000"
            android:layout_gravity="center" />
    </FrameLayout>

    <!-- Question -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Which of the objects in the picture above do you like?"
        android:textSize="18sp"
        android:fontFamily="@font/poppinssemibold"
        android:textColor="#333333"
        android:layout_marginBottom="12dp" />

    <!-- Image -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/object"
        android:scaleType="centerCrop"
        android:contentDescription="Objects to choose from"
        android:layout_marginBottom="12dp" />

    <!-- Instruction -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pin your answer in the image above"
        android:textSize="16sp"
        android:textColor="#666666"
        android:layout_gravity="center_horizontal"
        android:fontFamily="@font/poppinssemibold"
        android:padding="20dp"
        android:layout_marginBottom="24dp" />

    <!-- Submit Button -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/poppinssemibold"
        android:text="Submit"
        android:backgroundTint="#9C27B0"
        android:textColor="#FFFFFF"
        android:textStyle="bold" />
</LinearLayout>
